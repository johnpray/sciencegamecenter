<%= provide(:title, 'Subjects') %>
<h1>Subjects</h1>

<p>Click a subject name below to see all games that cover that subject.</p>

<ul class="taggings">
	<% @taggings.each do |t| %>
	<li><%= link_to t.tag.name, games_path(subject: t.tag.name) %></li>
	<% end %>
</ul>

