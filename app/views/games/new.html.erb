<%= provide(:title, 'New game') %>
<h1>New game</h1>

<div class="row">
  <div class="span6 offset3">

    <div class="form-description">
      <p>Use this form to add a new game to our authoritative list.</p>
    </div>

    <%= form_for(@game) do |f| %>

    	<%= render 'shared/error_messages', object: f.object %>

      <%= f.label :title %>
      <%= f.text_field :title %>
      <p class="form-help">The game's full title</p>

      <%= f.label :description %>
      <%= f.text_area :description %>
      <p class="form-help">A description of the game (facts only; no opinions -- save those for the reviews)<br/>Format using HTML tags or <a href="http://daringfireball.net/projects/markdown/basics">Markdown</a>: **bold** _italics_ [link](http://example.com)</p>

      <%= f.label :website_url, "Website URL" %>
      <%= f.text_field :website_url %>
      <p class="form-help">The full address (including <i>http://</i>) for the game's official web site.</p>

      <%= f.label :developer, "Developer" %>
      <%= f.text_field :developer %>
      <p class="form-help">Name and link to developer's website<br />using Markdown, something like [FAS](http://www.fas.org/)</p>

      <p class="form-help" id="submit-help"><b>Be aware:</b> Upon saving, this game will immediately be visible on the site.</p>
      <%= f.submit "Create game", :class => "btn btn-large btn-primary" %>
      
    <% end %>
  </div>
</div>