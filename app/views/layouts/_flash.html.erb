<% flash.each do |key, value| %>
  <% if !value.nil? && !value.empty? && key != :recaptcha_error %>
  <div class="alert alert-<%= key %>">
    <%= value %>
    <a class='close' data-dismiss='alert' href='#'>&times;</a>
  </div>
  <% end %>
<% end %>