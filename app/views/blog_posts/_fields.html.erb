<%= f.label :title %>
<%= f.text_field :title %>
<p class="form-help">The blog post's title. (The URL slug will be auto-generated from it.)</p>

<%= f.label :content %>
<%= f.text_area :content, id: 'blog_post_content', rows: 50 %>
<p class="form-help">Content/body of the post.</p>

<%= f.label :topic_list, "Topics" %>
<%= f.text_field :topic_list, value: f.object.topic_list.join(", ") %>
<p class="form-help">Topics that this post deals with, comma-separated.</p>

<%= f.label :published_at %>
<%= f.datetime_local_field :published_at %>
<p class="form-help">When this post was/will be published, in Eastern Time (US & Canada). Set it to now or the past to make it visible to all site visitors. Set it to the future to schedule future publishing. Make it blank to keep it hidden to regular site visitors.</p>

<%= f.label :pinned, "#{f.check_box :pinned} Pinned".html_safe %>
<p class="form-help">Check this to pin this post to the top of the homepage.</p>

<%= f.label :use_as_game_jam_page, "#{f.check_box :use_as_game_jam_page} Use as Game Jam page".html_safe %>
<p class="form-help">If checked, the Game Jam links and the /jam URL will redirect to this post. If enabled for multiple posts, it will use the most recently published one.</p>

<%# Set up WYSIWYG editor, TinyMCE %>
<script src="/tinymce/tinymce.min.js" referrerpolicy="origin"></script>
<script>
  console.log("Setting up TinyMCE editor...")
  tinymce.init({
    selector: '#blog_post_content',
    plugins: 'lists code link hr codesample image',
    menubar: false,
    toolbar: 'undo redo | styles | bold italic | link image | blockquote codesample hr | bullist numlist | code'
  });
</script>
